# CI minima da Fase 0: lint + testes do lado Python.
# Build de imagens, lint do web e eval em CI: T-027 / T-042.
name: ci

on:
  push:
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      # Sem o extra `eval` de proposito: MediaPipe/OpenCV pesam ~200 MB e os testes da
      # bancada rodam com extractor injetado. Eval em CI e a T-042.
      - name: Instalar dependencias
        run: uv sync --frozen --extra server

      - name: ruff
        run: |
          uv run ruff check .
          uv run ruff format --check .

      - name: pytest
        run: uv run pytest

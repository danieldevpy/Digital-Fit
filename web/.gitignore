node_modules/
dist/
.vite/

# Assets baixados/copiados por `npm run setup` (nunca versionados)
public/wasm/
public/models/
# Gerado pelo mesmo setup, a partir dos dois de cima (T-071).
public/pose-assets.json
dist-ssr/
